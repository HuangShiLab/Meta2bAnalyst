# Meta2bAnalyst 工作报告 — 2026-08-15

> 前序文档：[`CODE_REVIEW_2026-08-14.md`](CODE_REVIEW_2026-08-14.md)、[`FIXES_APPLIED_2026-08-14.md`](FIXES_APPLIED_2026-08-14.md)
> 本次提交：`e070cf9` → `d65d507`（8 个语义化 commit）

---

## 1. 本次完成的工作

### 1.1 阻塞性 Bug 修复

| Bug | 位置 | 影响 | 修复 |
|---|---|---|---|
| 弯引号 `“...”` 混入字符串 | `frontend/src/pages/Upload.tsx:176` | Vite 转换 500，**所有页面**加载报错 | 改直引号；全仓扫描确认唯一 |
| sklearn 二分类单行系数 | `backend/app/services/songbird.py` | 两组数据跑 Songbird 必崩 `IndexError` | ±w/2 对称展开为逐类布局，log-ratio 与二分类 log-odds 一致 |
| AgentChat 陈旧闭包 | `frontend/src/components/shared/AgentChat.tsx` + `useAgentInterpretation.ts` | 第一个问题永远拿到"请先等我解读"的复读回答 | hook 返回新鲜结果，组件用返回值而非旧 state |
| 16 条 Agent 关键词规则悬空 | `planner.MODULE_KEYWORDS` ↔ `MODULE_REGISTRY` | 用户提到 anosim/wgcna/rarefaction 等 16 种分析时无法规划 | 全部完成 executor 接线 + 注册（见 1.2） |
| `.gitignore` 规则误伤 | 根目录 `.gitignore` | 未锚定的 `test_*.py`/`run_*.py` 使 **11 个测试文件和服务器入口 `run_server.py` 从未进过版本库**，新克隆无法构建（3 个前端被引用文件也未跟踪） | 规则锚定根目录；补交全部遗漏文件 |

### 1.2 Agent 模块体系：16 个待接入模块全部上线

注册模块 **22 → 38**，关键词规则与注册表零漂移（启动告警已消失）：

- 直接接线（服务已存在且有 API 端点验证）：anosim、random_forest、heatmap、aldex2、songbird、enterotype、rarefaction、taxonomy_bar、mofa、diablo、wgcna、source_tracking、unifrac（phylogenetic_analysis）
- 新实现：**UpSet 分析**（`app/services/upset_plot.py`，按组 prevalence 集合交集 + Plotly 图）
- 视图型：volcano 接为 marker 结果的火山图视图；heatmap/unifrac/source_tracking 包装器补齐 `plot_data` 输出供报告生成器消费
- 规划器 2 个旧契约测试更新：断言"wgcna 现在可规划"，unavailable 契约改用 monkeypatch 保持覆盖

### 1.3 外部 LLM 接入（Kimi）

- `backend/.env` 配置新 key（gitignored，600 权限）；`config.py` 新增 `KIMI_API_KEY/KIMI_BASE_URL/KIMI_MODEL`
- 该 key 属 **Kimi for Coding 网关**（`api.kimi.com/coding/v1`，模型 `kimi-for-coding`，K2.7，256K 上下文）
- 适配两个网关限制：推理模型只接受 temperature=1（改为省略该参数）；推理 token 计入 max_tokens（提到 6000）
- **真实 API 调用验证通过**：`enhance_narrative` 返回了忠实于统计量（p=0.22 不显著）的专业改写
- 注意：shell 中若有 `KIMI_BASE_URL` 环境变量会覆盖 .env，启动时需 `env -u KIMI_BASE_URL`

### 1.4 知识库扩充

- 物种 60 → **80**：新增口腔/皮肤/阴道生态位（P. gingivalis、S. mutans、L. crispatus、G. vaginalis、C. acnes 等）与肠道共菌
- 疾病 15 → **25**：IBS、代谢综合征、高血压、ASD、MS、乳糜泻、哮喘、细菌性阴道病、牙周炎、龋齿
- 合并脚本 `backend/scripts/expand_kb_2026_08_15.py`：只增不覆盖，可重复执行

### 1.5 验证矩阵（全部通过）

| 层 | 验证 | 结果 |
|---|---|---|
| 单元/契约 | `pytest tests/` | **325 passed, 0 failed**, 4 skipped |
| 端到端（真实数据） | `backend/scripts/pipeline_smoke.py`（Huang_mBio 261 样本） | **62 PASS / 0 FAIL**：upload→alpha/beta/PCoA/NMDS/PERMANOVA/ANOSIM/RF→rarefaction→taxonomy→差异→多组学整合→多站点→agent 全家→PDF/CSV export |
| 新模块 | `backend/tests/manual_smoke_new_modules.py` | 15/15 真实执行 OK |
| 前端 | `tsc -b` / `oxlint` / `vite build` | 0 error / 0 error / 成功 |
| AgentChat E2E | `frontend/agent_e2e.mjs`（真实会话 7ada3f06 + interpret-full 回环） | 通过，首问即出答案 |

### 1.6 仓库卫生

- 删除 9 个误建空目录（`core,`、`types}` 等 brace-expansion 事故残留）
- 42 个一次性 `fix_*/test_*/check_*` 脚本归档 `scripts/legacy/`（不删除，可追溯）
- 页面截图在 `frontend/screenshots/`（未入库，可再生：`node frontend/screenshot_pages.mjs`）

---

## 2. 三个架构问题的评估

### 2.1 知识库：300+ 篇口腔微生物文献如何变成 KB 条目？

现状 KB 是手工策展 JSON（80 taxa / 25 diseases）。300+ 篇文献必须走**半自动文献挖掘管线**，纯手工不可持续：

```
PDF → 文本抽取(pypdf) → 章节定位(Methods/Results/Discussion)
    → LLM 结构化抽取（taxon × disease × direction × evidence 四元组，JSON schema 约束输出）
    → 证据分级（样本量 / 队列 vs 机制 / 是否多队列重复）
    → staging_kb.json（暂存区，不直接进库）
    → 人工审核队列 → 合并进 taxon_db / disease_db
```

关键设计：

1. **溯源字段**：每条关联带 `{pmid, year, cohort_size, direction, evidence_level}`——现在 `disease_associations` 只有一个方向词，文献驱动后必须升级 schema 存 provenance
2. **冲突处理**：同一 taxon-disease 关联按证据等级加权投票（meta-analysis > 多队列 > 单队列 > 体外），方向写 "enriched/depleted/mixed" 的 mixed 由投票产生而非拍脑袋
3. **LLM 成本**：已配好的 kimi-for-coding（256K 上下文）单篇全文一次调用可装下；300 篇 ≈ 300-600 次调用，成本可控
4. **诚实性兜底**：沿用 PaperWriter 的原则——LLM 只做"抽取与改写"，条目进库前必须过 schema 校验 + 人工审核，抽取失败的论文进失败清单而不是编造

### 2.2 Agent 能力是否可充分定义和扩展？

**扩展性已无架构障碍**：注册契约 = `ModuleSpec`（声明 I/O、参数、约束）+ executor 映射（一个函数），两步即可新增模块，本次 16 个模块就是按这个契约批量接入的。真正要补的是**自然语言层**：

**(1) 用户不熟悉固定流程 → 自然语言了解与操控**

- 现状：规划器是规则+关键词，用户说"人话"命中不了关键词就会得到 clarification。缺一个 **LLM fallback**：规则规划失败时，把 `MODULE_REGISTRY` 的 38 个模块描述喂给 LLM，让它输出结构化 JSON（模块名+参数），**再用 registry 校验**——LLM 提议的模块不存在就拒绝，绝不规划不可执行的步骤。这在架构上是给 `_detect_modules_from_keywords` 加第二层，改动集中在 planner 一处
- 再加一个**反向能力**"解释这个流程"：给定模板/计划，用 LLM 生成大白话的逐步说明（每步做什么、为什么、输出什么图），解决"不熟悉流程"的认知门槛

**(2) 上传文献 → 复现文献的分析流程（paper-to-pipeline）**

这是本平台最有差异化的方向，且地基已齐：

```
用户上传 PDF → 抽取 Methods 段
→ LLM 将文献描述的分析映射到 MODULE_REGISTRY（分析类型、检验方法、阈值、参考组）
→ 生成 ExecutionPlan 草案（复用现有 PlanStep/依赖校验）
→ 前端展示"文献流程 vs 平台模块"对照表，用户确认/调整 → 执行
```

- 关键约束：**文献里有而平台没有的方法必须显式标注"平台暂不支持"**，不能静默替换成别的方法——这是诚实性契约的自然延伸
- 工作量集中在新端点（如 `/api/v1/agent/plan-from-paper`）+ 提示词工程 + 一个确认 UI；plan 校验器、执行器、SSE 流都是现成的

**(3) KB + 自然语言交互 → 完成写作**

已有一半：`PaperWriter`（规则式、占位符诚实策略）+ `llm_client.enhance_narrative`（KB 事实约束下的改写）。缺口：

- `interpret-full` 目前不走 LLM（前端 `use_llm: false` 写死）——应改为开关，默认开，失败自动降级 KB-only（客户端已实现降级）
- 写作从"段落改写"升级到"章节生成"：Results 段由统计结果驱动（已有 p 值驱动措辞），Discussion 段由 `find_taxa_by_disease` / `lookup_method` 的 KB 命中驱动，LLM 只负责组织语言
- 所有 LLM 产出保留 `ai_generated` / `requires_author_verification` 标记（PaperWriter 已有此字段）

---

## 3. 下一步计划（按优先级）

| 优先级 | 事项 | 依据 |
|---|---|---|
| P0 | **文献挖掘管线**：`backend/scripts/literature_mine.py`（PDF→四元组抽取→staging→审核合并）+ KB schema 增加 provenance 字段 | 300+ 篇口腔文献是现成资产；2.1 的全部设计为此服务 |
| P0 | **推送 GitHub**（当前 8 个 commit 只在本地）+ CI（pytest + pipeline_smoke 门禁） | 防止再次丢失测试/入口文件 |
| P1 | **规划器 LLM fallback** + "解释这个流程"模式 | 直接解决 2.2(1) |
| P1 | **interpret-full 接入 LLM 开关**（前后端各一行改动，降级已实现） | 立即提升 Agent 回答质量 |
| P2 | **plan-from-paper**：文献 Methods → ExecutionPlan 草案 + 确认 UI | 2.2(2)，差异化功能 |
| P2 | **Discussion/Results 章节生成**接 KB 驱动 | 2.2(3) |
| P3 | 前端 22 个 oxlint warning 清理；截图脚本接入 CI | 卫生项 |
